# 修改过程记录

日期 2026.09.23
范围 aiTools 扩展 1.0 到 1.1，删除 popup、整体评审、九项改进

## 一、删除 popup 并让图标直开侧边栏

1. 读取 manifest.json、popup.html、popup.js、background.js，确认 popup 只有两个作用，一个是快捷提示词开关，一个是打开侧边栏的中转按钮。
2. 从 manifest.json 的 action 中删除 default_popup 字段。
3. 在 background.js 顶部新增 chrome.action.onClicked 监听，拿到当前窗口 id 后调用 chrome.sidePanel.open。
4. 删除 popup.html 与 popup.js。
5. 把 popup 里的快捷提示词开关迁进侧边栏，功能设置页顶部新增全局卡片，写入 showPromptButtons 并广播给标签页。
6. 导出配置带上 showPromptButtons，导入时一并恢复，恢复默认也重置它。
7. 修复过程中暴露的两个老问题。
   1. 功能开关的选择器从 .feature-item input 收紧为带 data-site 与 data-feature，否则新卡片会被误当成站点开关。
   2. getSettings 之前直接返回 DEFAULT_CONFIG 引用，开关一改就把默认配置就地篡改，恢复默认失效，改为读写都深拷贝。
   3. 静态控件的监听器改为只绑定一次，反复切换视图不再叠加确认框。
8. 写测试断言 6 项全部通过，该文件后续演进为 tests/sidepanel.test.js。

## 二、整体评审

1. 通读 manifest、sidepanel、utils、全部内容脚本与 Hacker News 样式表。
2. 用脚本实测站点匹配，得出 key grok、pinterest、laoqian 三个开关永远不会命中的结论。
3. 结论汇总为九项改进路线，按依赖顺序执行，先配置层，再内容脚本，最后工程规范。

## 三、九项改进落地

### 1. hosts 显式匹配

1. 新建 defaults.js，存放 STORAGE 键名、DEFAULT_PROMPTS、DEFAULT_CONFIG、cloneConfig、mergeSettings、matchSite、readState。
2. 每个站点声明 hosts，支持子域名匹配，例如 hosts 填 behance.net 可以命中 www.behance.net。
3. mergeSettings 负责把存量配置合并回默认结构，补齐 hosts、补新站点、丢弃已废弃功能、选项列表以默认配置为准。
4. 所有内容脚本与 sidepanel 共用这一份配置，manifest 中每个 js 数组以 defaults.js 开头。

### 2. 配置热更新

1. 删除十处 location.reload。
2. utils.js 新增 AIToolsUtils.onSettingsChanged，监听 chrome.storage.onChanged，只关心 aiToolsSettings 与 showPromptButtons 两个键。
3. 每个脚本实现 apply 函数，同时覆盖开启与关闭两个方向。
   1. 纯样式类脚本用 applyStyle 与 removeStyle 增删 style 节点。
   2. 老钱博客直接设置与还原内联背景色。
   3. Behance 记录原始内联定位，关闭时原样恢复。
   4. 三个提示词脚本按配置与提示词签名决定按钮栏的创建与移除。
4. sidepanel 不再向标签页广播消息，chrome.tabs 权限随之可以删除。

### 3. 权限收敛

1. manifest 权限只保留 storage、downloads、sidePanel。
2. 删除 scripting、clipboardWrite、activeTab、tabs 四个没有代码引用的权限。

### 4. 设置页覆盖全部注入站点

1. 新增 hackernews、gemini、speedlab 三个站点条目。
2. medium 的第二个匹配规则补上通配，否则只命中根路径。
3. 老钱博客的内容脚本补上 darkBackground 子开关判断。
4. ChatGPT 删除永远不生效的 hideFloating 功能，因为脚本只注入在 chatgpt.com 上，同时新增 quickPrompts 开关，三个提示词站点行为对齐。

### 5. Hacker News 修复与设置界面

1. keydown 加上输入框与输入域判断，焦点在 input、textarea、select、contenteditable 内直接返回。
2. 设置页新增功能下拉框渲染，feature 增加 type 为 select 时渲染 select，并新增 spacing 与 theme 的取值保存分支。
3. Hacker News 提供字体、行距、主题三个下拉框与快捷键、外链新标签两个开关。
4. 基础样式从 manifest 的 css 移到脚本注入 link，站点开关关闭时能真正卸载样式还原原生页面。
5. styles.css 的字体地址改为相对路径，声明 Bold 与 SemiBold 两个本地字体，删除 fonts.googleapis.com 在线请求。
6. 删除 forceRoundedFont、applyRoundedFontStyles 的全元素遍历、addLoadingAnimations 空函数、updateSettings 死监听与四个遗留 storage 键。

### 6. 存储与安全

1. 配置与提示词改存 storage.local，写入走 Promise 风格，失败直接抛出。
2. background.js 新增 migrateStorage，在 onInstalled 与 onStartup 时把 sync 旧数据搬到 local，本地已有数据时不覆盖。
3. readState 读取时本地缺失仍回退 sync，保证迁移期间内容脚本可用。
4. sidepanel 新增 escapeHtml，站点名称、功能名称、功能描述、提示词名称与提示词内容全部转义。
5. 删除空状态里的 emoji 图标。

### 7. Pinterest 行为修正

1. 配置检查提前到地址改写之前，关闭开关时不再强制跳转。
2. 下载前查 downloadedUrls 去重，成功后记录地址，最多保留 100 条。
3. 下载标记放在收到成功响应之后，失败会放开标记允许重试。

### 8. 死代码清理

1. 删除 brightness_controller.js 与空的 todo.md。
2. utils.js 删除 waitForElement、observeDOMChanges、initWithRetry、createButtonBar 四个无人调用的导出。
3. DEFAULT_PROMPTS 只保留 defaults.js 一份，utils 的兜底改为克隆它。
4. 删除无效的 :contains 选择器，Medium 与 Perplexity 的冗余 observer 改为纯样式方案。
5. readme 的项目结构修正，删除不存在的 docs 目录，补上 defaults.js、tests、eslint 配置。

### 9. 工程规范

1. 新增 eslint.config.js，规则含 no-var、prefer-const、eqeqeq、no-unused-vars，共享全局只对 defaults.js 之外的文件生效，避免 no-redeclare。
2. 新增 .editorconfig 与 .gitattributes，全仓行尾统一 LF，gg.bat 单独声明 CRLF。
3. 用 perl 把工作区文本文件的 CRLF 统一转成 LF，仓库里那批假性修改随之消失。
4. manifest version 与 package.json version 升到 1.1 与 1.1.0。
5. readme 按文档规范重写，删除加粗、括号、双引号，补 2026.09.23 更新日志与权限说明、测试命令。

## 四、测试与检查

1. 新增 tests/helpers.js，提供 chrome API 模拟、页面加载器与 defaults.js 加载器，storage 采用 Promise 风格并触发 onChanged。
2. tests/manifest.test.js 共 6 项，校验无 popup、权限收敛、脚本文件存在且以 defaults.js 开头、域名匹配规则、Hacker News 样式注入与可访问资源、代码里不再出现 location.reload 与 chrome.storage.sync 直连、死文件与已下线站点引用已清理。
3. tests/config.test.js 共 7 项，校验每个站点的 hosts、真实注入域名都能匹配、子域名与无关域名、存量配置合并、下拉框选项防篡改、已下线站点的存量条目被丢弃、默认配置不共享引用。
4. tests/content.test.js 共 13 项，覆盖内容脚本的开关增删、热更新不刷新页面、提示词按钮增删、自动测速、快捷键劫持、基础样式卸载、发布时间落点、地址改写门控与下载去重。
5. tests/sidepanel.test.js 共 8 项，校验渲染、站点开关落盘、不写 sync、不广播标签页、下拉框持久化、注入转义、恢复默认、静态监听器不叠加。
6. tests/background.test.js 共 4 项，校验图标开侧边栏、配置迁移与不覆盖、本地缺失时回退 sync、下载消息返回成功。
7. tests/styles.test.js 共 3 项，校验 Hacker News 全局字重、标题链接色、作者色与本地字体路径与旧版渲染一致。
8. 执行结果。
   1. npm run lint，零错误。
   2. npm test，41 项全部通过，0 失败。
   3. node --check 逐个校验 defaults.js、background.js、sidepanel.js 与全部内容脚本，无语法错误。

## 五、遗留确认

1. 知乎的隐藏操作栏选择器包含 .RichContent-outputText，它在知乎是正文容器，可能把文章正文一起隐藏。没有在真实页面验证过，本次未改动，等确认后再处理。

## 六、生效方式

1. 打开 chrome://extensions。
2. 点击 aiTools 的重新加载按钮。
3. 首次启动会自动把 storage.sync 上的旧配置迁移到 storage.local。
4. 执行 npm run lint 与 npm test 复验。

## 七、2026.09.24 追加修改

### 1. 删除 Medium 全部功能

1. 删除 manifest.json 里 medium.com 与 levelup.gitconnected.com 的 content_scripts 条目。
2. 删除 defaults.js 中的 medium 站点与 hideFloat 功能项。
3. 删除 content_scripts/medium_content.js。
4. mergeSettings 改为直接丢弃存量配置里已下线的站点，避免设置页残留没有内容脚本的死开关。
5. 测试同步删除 Medium 用例，改为断言 manifest 与配置中不再出现 medium，并新增存量 Medium 条目被丢弃的用例。
6. readme 删除 Medium 功能小节与结构中的文件条目，补 2026.09.24 更新日志。

### 2. 上一轮改动的复核结论

1. 把 1.0 的渲染结果与 1.1 逐条比对，发现三处行为差异。
   1. Hacker News 旧版在 DOMContentLoaded 时注入 custom-font-style 与 custom-theme-style，注入位置晚于基础样式，同优先级下后写的规则生效，实际渲染是全局字重 600、标题链接色 1a4480、作者名色 ff6600。上一轮删掉这两个 style 后回落到基础样式的 700、1d1f57、3c963c，属于视觉回退，已在 styles.css 改回旧值并补 styles.test.js 断言。
   2. 知乎发布时间旧版只在页面没有作者栏时才写到文章头部，上一轮改成两处都写，同页会出现两个时间，已改回原条件并补测试。
   3. Medium 的动态节点额外有一条 classList 单 token 判断，站点功能整体删除后该差异不复存在。
2. 复核确认未回退的部分：popup 的提示词开关、三个提示词站点的按钮栏、Grok 焦点巡逻、Behance 定位还原、Speed Test 自动测速、Pinterest 下载与去重、Hacker News 快捷键与主题行距字体开关、侧边栏全部控件。

### 3. 其他清理

1. 删除只被 popup 引用的 images/g1.png。

### 4. Pinterest 自动下载失效排查与修复

1. 核对事实，逐道门读代码确认链路完整：manifest 注入条目在、downloads 权限在、后台下载消息在。
2. 定位到上一轮引入的两道新门。
   1. 下载去重原先按永久记录，同一张图第二次打开直接静默跳过，只有一行日志，看起来就是功能坏了。
   2. 站点开关与下载原图子开关上一轮才修活，旧版因为站点 key 匹配不到 i.pinimg.com 一直是摆设，历史上关过的话，现在会真的停掉功能。
3. 修复。
   1. 去重改为十分钟窗口，窗口之外重新打开仍然下载。
   2. 旧格式的纯字符串记录一律视为过期，已记录的地址立刻恢复下载。
   3. 每道门补控制台日志，站点开关关闭、子开关关闭、地址格式不匹配、窗口内跳过并打印剩余分钟、请求下载、下载失败放开重试，都会打印明确原因。
4. 测试。Pinterest 用例拆成两个，覆盖开关关闭不改写不下载、窗口期内去重、旧格式记录重新下载、过期记录重新下载，共 41 项全部通过。

### 5. 新增 gg.sh 一键推送

1. 新建 gg.sh，行为对齐 Windows 下的 gg.bat，切换到 main 分支，提示输入提交信息，暂存全部改动，提交并推送到 origin，最后打印状态。
2. 支持 ./gg.sh "提交信息" 直接带参，提交信息为空或全空格时直接取消，不产生空提交。
3. 赋予可执行权限，bash -n 通过语法检查，用临时裸仓库验证了从暂存到推送的完整流程，没有对真实仓库执行任何 git add、commit、push。

### 6. 文档检查与更新

1. readme 补当前版本、gg.sh 与 gg.bat 的结构条目、一键提交推送小节、storage 迁移说明、测试用例数 41，Pinterest 小节改为十分钟窗口去重并注明控制台日志，2026.09.23 条目去掉会过期的用例数字。
2. 本文件修正各测试文件的用例数，补 tests/styles.test.js 说明，把已过期的 37 项与 40 项统一为当前 41 项，重排第七节的编号，让当前验证排在最后。
3. package.json 补 description，删除指向不存在文件的 main 字段。

### 7. 当前验证

1. npm run lint，零错误。
2. npm test，41 项全部通过，0 失败。
